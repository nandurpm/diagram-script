{
  "name": "diagram-script",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Small offline DSL for technical flowcharts and block diagrams.",
  "bin": {
    "diagram-script": "./src/cli.mjs"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/*.mjs",
    "demo": "node src/cli.mjs render examples/voltage-check.diagram --format html --out reports/demo/voltage-check.html",
    "start": "node src/render-host.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
